# Sanity read-only client (Phase 1 pilot). Copy to .env and fill in.
# The project ID is not a secret, but keep real values out of git anyway.
# NEVER put a Sanity token in this file or in .env for the site build —
# published public content requires none.
SANITY_PROJECT_ID=your-project-id
SANITY_DATASET=production
