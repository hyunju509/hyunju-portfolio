# Copy to .env and fill in. The project ID is visible in every API request
# and is not a secret, but real values stay out of git by convention.
# NEVER put an API token in this file — Studio auth is your Sanity login.
SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production
